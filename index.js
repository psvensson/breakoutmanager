class BreakoutManager
{
  // The manifest either is the object inside or the url to an npm package.json or bower bower.json that defined a specific package
  static transformLinks(manifest) {
    // Get manifest

    // Follow the dependency tree and download all manifests, building a hashtable of all resource names and their base URLs

    // For the original manifest

    // Identify main file to load

    // Transform each relative path into full URI that links to the source repository of the referenced resource
    
    // For each resource file, repeat the above, recursively for any other files referenced et.c. until we reach the end of the tree.

    // return transformed HTML text

  }
}