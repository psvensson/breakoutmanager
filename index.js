class BreakoutManager
{
  static transformLinks(manifest, htmltext) {
    // Detect if bower or npm manifest

    // Find all script tags or import links

    // Transform each relative path into ful URI that links to the source repository

    // return transformed HTML text
    
  }
}